fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
supabase==2.10.0
# Pin supabase's transitive deps explicitly. Production's cached build env
# was caught running storage3 v0.12.1 (impossible under supabase==2.10.0,
# which requires <0.10) because `pip install -r` treats an already-installed
# supabase==2.10.0 as satisfied and never re-validates its transitive pins.
# Explicit top-level pins force pip to correct a drifted cached env on the
# next build. These are the exact versions the test suite runs against.
storage3==0.9.0
postgrest==0.18.0
gotrue==2.12.4
supafunc==0.7.0
realtime==2.5.3
httpx==0.27.2
PyJWT==2.10.1
pytest==8.3.4
python-multipart==0.0.20
openai>=1.93.0
openpyxl==3.1.5
